name: 'jaffle_shop'

profile: 'jaffle_shop'

models:
  jaffle_shop:
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: core
    staging:
      +materialized: view
      +schema: staging
